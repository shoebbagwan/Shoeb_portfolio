import type { Metadata } from "next";
import { Space_Grotesk, Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const spaceGrotesk = Space_Grotesk({
  variable: "--font-space-grotesk",
  subsets: ["latin"],
  weight: ["500", "600", "700"],
});

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["400", "500", "600"],
});

const jetbrainsMono = JetBrains_Mono({
  variable: "--font-jetbrains-mono",
  subsets: ["latin"],
  weight: ["400", "500"],
});

export const metadata: Metadata = {
  title: "Shoeb Bagwan — AI/ML Engineer",
  description:
    "AI/ML Engineer building Generative AI, NLP, RAG, and speech systems with Python, LangChain, and AWS. B.E. Computer Science (AI & ML), Mumbai.",
  metadataBase: new URL("https://shoebbagwan.github.io"),
  openGraph: {
    title: "Shoeb Bagwan — AI/ML Engineer",
    description:
      "Generative AI, NLP, RAG, and speech systems — built end to end and shipped.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${spaceGrotesk.variable} ${inter.variable} ${jetbrainsMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
